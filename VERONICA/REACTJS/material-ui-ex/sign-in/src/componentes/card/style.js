import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    root: {
      maxWidth: 340,
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
    },
     media:{
      height:140,
    }
    
  }));